import 'package:flutter/material.dart';

class SerachI extends StatelessWidget {
  const SerachI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
